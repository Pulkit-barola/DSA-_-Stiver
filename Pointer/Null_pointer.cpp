#include<bits/stdc++.h>
using namespace std;

int main(){
    int *ptr = NULL; // we assign null  because they does not point any location

    cout<<ptr<<endl;
    //derefracing of a null pointer is not possible
}